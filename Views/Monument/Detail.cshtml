@model Monument

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<main>
    <div class="d-flex justify-content-center container mt-5">
        <div class="card p-3 bg-white"><i class="fa fa-apple"></i>
            <div class="about-product text-center mt-2">
                <img style="border: 2px solid black; margin-bottom: 15px;" src="@Model.Image" width="500">
                <div>
                    <h4>@Model.Name</h4>
                    <h6 class="mt-0 text-black-50">@Model.Category.Name</h6>
                </div>
            </div>
            <div class="stats mt-2">
                <p>Miasto: @Model.City.Name</p>
                <p>Opis: @Model.Descripton</p>
                @{
                    string[] source = Model.Image.Split("/");
                    if (source.Length>=3)
                }
                <p class="mt-0 text-black-50">Źródło: @source[2]</p>
            </div>
        </div>
    </div>

<p></p>
</main>